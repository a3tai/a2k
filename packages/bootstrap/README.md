# @a2k/bootstrap

Produces deterministic, review-only local agent configuration plans. The current scaffold never writes files. Client-specific writers require complete-diff review and explicit approval.
