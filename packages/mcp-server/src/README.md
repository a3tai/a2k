# A2K MCP server scaffold

This package currently publishes the proposed read-only MCP tool contracts. It does not yet open a transport, read a checkout, follow remote references, or mutate knowledge.

A later implementation will use the stable v1 MCP TypeScript SDK. Remote Streamable HTTP and OAuth/OIDC support require a separate security review; stdio remains the first reference binding.
