# @a2k/validator

Local, fail-closed validation for A2K YAML manifests. It rejects duplicate keys, aliases, multiple documents, excessive input, unsupported versions, and schema violations. It performs no network requests and follows no references.
